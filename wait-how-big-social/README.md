# Wait, How Big? — public media branch

This branch exists only to host original, generated media at stable public raw URLs for the zero-budget social publishing experiment. It does not modify the orchestrator default branch.

The workflow renders 13 vertical MP4 files, matching covers, six carousel sets, and an asset verification report. No credentials or private user data are stored here.
