<!-- Keep it short — see CONTRIBUTING.md for conventions. -->

## What / why



## Test plan

- [ ] `make test-strict` passes (`python3 -W error::ResourceWarning -m unittest discover -s tests -q`)
- [ ] `make verify` passes, if this touches the GUI (macOS only)
- [ ] Added/updated tests for any behavior change
