version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "monthly"
  # The engine itself is stdlib-only; this watches the dev extras (ruff, mypy).
  - package-ecosystem: "pip"
    directory: "/"
    schedule:
      interval: "monthly"
