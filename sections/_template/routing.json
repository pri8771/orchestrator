{
  "_comment": "Fleet artifact-routing defaults. Sections and projects may shadow a match by artifact type + source section.",
  "artifact_routes": {"failure": "notification"},
  "rules": [
    {"rule_id": "failure-to-notification", "match": {"artifact_type": "failure"}, "strategy": "one", "targets": ["notification"], "hop_budget": 4}
  ]
}
